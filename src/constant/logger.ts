export enum LOG_ENVIRONMENTS {
    console = "default",
    file = "file",
}

export enum LOG_LEVEL {
    off = "OFF",
    fatal = "FATAL",
    error = "ERROR",
    warn = "WARN",
    info = "INFO",
    debug = "DEBUG",
    trace = "TRACE",
    all = "ALL",
}
